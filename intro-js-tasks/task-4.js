// Task-4
// What will be the result of the following codes:

var a = isNaN("11");

var b = isNaN(2 - 10);

console.log(a, b);

// Explain your answers.

// The string "11" can be easily converted to the number 11.
// Since 11 is a valid number, "a" will be "false"

// The result from b is -8. It is also a valid number, so "b" will be "false" too.
